package com.abstractphil.absitem.commands;

import org.bukkit.command.Command;
import org.bukkit.command.CommandExecutor;
import org.bukkit.command.CommandSender;

public class AbsLvlLegacyCommand implements CommandExecutor {

    // Contains the prisons level code in a new form, revamped to allow for levelups on any reditem internally.
    @Override
    public boolean onCommand(CommandSender commandSender, Command command, String s, String[] strings) {

        return false;
    }
}
